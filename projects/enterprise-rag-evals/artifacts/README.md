# Evaluation artifacts

`evaluation.json` is generated by:

```powershell
python -m app.cli eval --output artifacts/evaluation.json
```

It is intentionally small and versioned as evidence of the synthetic fixture run.
It must not be read as a production accuracy or latency claim.
